packagesDir: ./charts
outputDir: ./release
chartsRepo:
  git: github.com/JZacharie/opengist-chart.git
  branch: main
helm:
  chartDirs:
    - .
github:
  token: ${{ secrets.CR_TOKEN }}
  cr_token: ${{ secrets.CR_TOKEN }}
  pagesBranch: gh-pages
  userName: jzacharie
  userEmail: joseph@zacharie.org
